name: Build and Release

on:
  push:
    branches: [master, main]
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build extension
        run: npm run compile

      - name: Package extension
        run: npx vsce package

      - name: Get package info
        id: package
        run: |
          VERSION=$(node -p "require('./package.json').version")
          NAME=$(node -p "require('./package.json').name")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "vsix_file=${NAME}-${VERSION}.vsix" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: vsix-package
          path: '*.vsix'
          retention-days: 30

      - name: Create or Update Release
        uses: softprops/action-gh-release@v1
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: v${{ steps.package.outputs.version }}
          name: Auto Accept Agent v${{ steps.package.outputs.version }}
          draft: false
          prerelease: false
          files: ${{ steps.package.outputs.vsix_file }}
          body: |
            ## 📦 Auto Accept Agent v${{ steps.package.outputs.version }}
            
            ### 安装方法
            
            1. 下载下方的 `.vsix` 文件
            2. 打开 Antigravity / VS Code / Cursor
            3. 按 `Ctrl+Shift+P`，输入 `Install from VSIX`
            4. 选择下载的 `.vsix` 文件
            5. 重启 IDE
            
            ### 功能特性
            - ✅ 自动接受 AI 建议
            - ✅ 智能快捷方式修补
            - ✅ 右键菜单注册表修补
            - ✅ 多实例支持
            
            ---
            *自动构建于 ${{ github.sha }}*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
